/** Minimum selection size in natural image pixels. */
export const MINIMUM_SELECTION_SIZE_NATURAL = 4;

/** Zoom bounds. */
export const MINIMUM_ZOOM_SCALE = 0.1;
export const MAXIMUM_ZOOM_SCALE = 20;

/** Multiplicative zoom step for one wheel notch or one toolbar click. */
export const ZOOM_STEP_FACTOR = 1.15;

/** Visual size of resize handles in displayed pixels (counter-scaled). */
export const HANDLE_SIZE_PIXELS = 12;

/** Visual border thickness in displayed pixels (counter-scaled). */
export const SELECTION_BORDER_PIXELS = 2;
